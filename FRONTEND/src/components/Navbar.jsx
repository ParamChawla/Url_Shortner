import { Link, NavLink, useNavigate } from 'react-router-dom'
import { Menu, X, Link2 } from 'lucide-react'
import { useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { useAuth } from '../context/AuthContext'
const links=[['Home','/'],['Dashboard','/dashboard'],['Analytics','/analytics'],['Pricing','/pricing'],['About','/about'],['Contact','/contact']]
export default function Navbar(){const [open,setOpen]=useState(false);const {user,logout}=useAuth();const n=useNavigate();const signout=async()=>{await logout();n('/')};return <header className="sticky top-0 z-50 border-b border-white/[.06] bg-ink/70 backdrop-blur-xl"><nav className="mx-auto flex h-18 max-w-7xl items-center justify-between px-5 py-4"><Link to="/" className="flex items-center gap-2 font-semibold tracking-tight"><span className="rounded-xl bg-gradient-to-br from-primary to-cyan p-2"><Link2 className="h-4 w-4"/></span>linkly</Link><div className="hidden items-center gap-6 lg:flex">{links.map(([x,to])=><NavLink key={to} to={to} className={({isActive})=>`text-sm transition ${isActive?'text-white':'text-gray-400 hover:text-white'}`}>{x}</NavLink>)}</div><div className="hidden items-center gap-3 sm:flex">{user?<button onClick={signout} className="text-sm text-gray-300">Sign out</button>:<Link to="/login" className="text-sm text-gray-300">Login</Link>}<Link to={user?'/dashboard':'/register'} className="rounded-xl bg-white px-4 py-2 text-sm font-semibold text-ink">{user?'Open app':'Get started'}</Link></div><button className="sm:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></nav><AnimatePresence>{open&&<motion.div initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden border-t border-white/10 px-5 sm:hidden">{links.map(([x,to])=><Link key={to} onClick={()=>setOpen(false)} className="block py-3 text-sm text-gray-300" to={to}>{x}</Link>)}</motion.div>}</AnimatePresence></header>}
